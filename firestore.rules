rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read and write access (for development/testing only)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
